import 'package:flutter/material.dart';

class PrayerTimeSettingScreen extends StatefulWidget {
  const PrayerTimeSettingScreen({Key? key}) : super(key: key);

  @override
  _PrayerTimeSettingScreenState createState() => _PrayerTimeSettingScreenState();
}

class _PrayerTimeSettingScreenState extends State<PrayerTimeSettingScreen> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
